# TODO

- Add an anti-synergy table to the Synergy page. The lift-scale shrinkage means scores < 1.0 now read as meaningful anti-synergy (e.g. 0.4 = pair co-occurs less than half as often as independence predicts), so the bottom of the distribution is just as interesting as the top.
